defaults:
  - data: ogb_dataset
  - model: gcn
  - _self_

training:
  lr: 0.001
  epochs: 20
  batch_size: 32
  optimizer: adam
  to_undirected: true

meta:
  owner_initials: "mo"

wandb:
  project: "politechnika-gnn-thesis"
  entity: "politechnika-gnn-thesis"
  run_name: "${meta.owner_initials}_${cfg.model.name}_${cfg.data.name}_{cfg.data.target}_${data.dataset.scenario}_ep${training.epochs}"
