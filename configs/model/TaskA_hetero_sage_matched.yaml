# Fair HeteroSAGE control with the same input projection as attention models.
name: hetero_sage_matched
conv_type: hetero_sage_matched
encoder_name: hetero_sage_matched

hidden_channels: 64
hidden_dim: 64
num_layers: 2
dropout: 0.2
residual: true
relation_aggr: sum
