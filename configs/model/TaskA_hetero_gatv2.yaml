# HeteroGATv2 — per-relation attention within HeteroConv.
name: hetero_gatv2
conv_type: hetero_gatv2
encoder_name: hetero_gatv2

hidden_channels: 64
hidden_dim: 64
num_layers: 2
heads: 4
dropout: 0.2
residual: true
relation_aggr: sum
