# HCR-3: classical binary association stats (control without HCR naming).
enabled: true
variant: classical_binary
output_dim: 3
smoothing: 0.5
fit_split: train
unsupported_pair_mode: zeros
unknown_pair_value: 0.0

features:
  risk_difference: true
  log_odds_ratio: true
  joint_support_rate: true
