# HCR-2: dependence + support without classical effect stats.
enabled: true
variant: binary_minimal
output_dim: 4
smoothing: 0.5
fit_split: train
unsupported_pair_mode: zeros
unknown_pair_value: 0.0

features:
  phi: true
  mutual_information: true
  joint_support_rate: true
  uncertainty: true
